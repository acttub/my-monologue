// 나만의 독백 — Vercel 서버리스 함수 (/api/generate)
// 2패스: 생성(Gemini) → 독립 검사(Gemini, 별도 호출) → 통과분만 반환. 실패 시 폴백 대본.
//
// 왜 2패스인가: 이 서비스는 모델 자유 생성 텍스트를 사용자에게 그대로 노출한다.
// 보이스 컬러처럼 "열거형만 통과"시키는 구조적 방어를 쓸 수 없으므로, 그 자리를 검사 층이 대신한다.
// 생성 프롬프트의 지시만으로는 FR-008(안전)·FR-017(패러프레이즈 탐지)을 보장할 수 없다.
// 상세: specs/001-my-monologue/research.md R-1
"use strict";

const FALLBACKS = require("../fallbacks.json");

// ── 선택 축 (구조적 검증: 이 값 밖은 받지 않는다) ────────────────────────
const LENGTH = ["sec30", "min1", "min2"];
const TARGET = ["leaving", "abandoned", "gone", "believed", "unknown-self"];
const HEAT   = ["burst", "hold", "persuade", "collapse"];
const TONE   = ["now", "classic"];

// 배포 전 감사에서 제거된 조합. 프롬프트로 막을 수 없어 조합 자체를 뺐다.
// gone × burst: "왜 나를 두고 갔느냐"는 분노가 고인을 비난 가능한 주체로 세우고,
// 그 비난이 자살 암시로 미끄러진다. 어휘를 막으면 완곡어로, 의미로 물으면
// 유서 구조로 빠져나갔다. 3회 수정에도 12/98까지 올라가 조합을 제거했다.
// 감사 기록: specs/001-my-monologue/audit-2026-07-19.md
const BLOCKED_PAIRS = [{ target: "gone", heat: "burst" }];

// 길이별 목표 글자수 (한국어 낭독 ≈ 초당 5음절). FR-003의 ±25%는 클라이언트 표기가 아니라 이 값 기준.
// 상한을 명시하지 않으면 모델이 넘긴다. 4차 감사에서 min2 관측치가 999자까지 나왔고
// (목표 600~800), 생성 시간은 출력 길이에 비례하므로 이게 곧 지연이다.
const LENGTH_SPEC = {
  sec30: { label: "30초", chars: "150~200자", max: 230 },
  min1:  { label: "1분",  chars: "300~400자", max: 450 },
  min2:  { label: "2분",  chars: "600~800자", max: 850 }
};

// 상대의 '물리적 위치'까지 못박는다. 이걸 흐리게 두면 abandoned가 leaving으로 흘러
// "아직 문 앞에 있는 사람을 붙잡는" 장면이 되어 선택과 어긋난다(2026-07-19 감사에서 20% 불일치).
const TARGET_KO = {
  leaving: "지금 떠나려는 사람. **상대는 아직 눈앞에 있다.** 아직 말이 닿는다",
  abandoned: "나를 버리고 이미 떠나버린 사람. **상대는 지금 이 자리에 없다.** 붙잡을 수 없고, 말은 허공에 간다",
  gone: "이미 세상에 없는 사람. **답이 돌아오지 않는다**",
  believed: "나를 믿어준 사람(스승·부모·선배 등). **상대는 나를 의심한 적이 없다** — 나를 의심한 사람으로 쓰지 마라",
  "unknown-self": "미래의 자기 자신. **수신자는 오직 나 자신이다** — 옛 연인이나 타인이 수신자가 되면 안 된다"
};
const HEAT_KO = {
  burst: "감정을 터뜨리는",
  hold: "말을 참고 눌러두는",
  persuade: "상대를 설득하려는",
  collapse: "버티다 무너지는"
};
const TONE_KO = {
  now: "지금 한국에서 실제로 쓰는 구어체",
  // classic이 무너지는 이유는 '옛스러운 말투'를 '옛스러운 상황·소품'으로 확장하기 때문이다.
  // 말투만 격식체이고 세계는 현대여야 한다 (2026-07-19 감사: classic 성립률 29%).
  classic:
    "격식 있는 문어체. **말투만 격식체이고, 상황과 소품은 현대여야 한다.** " +
    "시대극처럼 쓰지 마라. 편의점·지하철·카톡·월세·면접 같은 현대의 것이 문어체로 말해질 때 생기는 " +
    "긴장이 목적이다. '그대여'로 시작하는 호명, 미문(美文) 나열, 탄식조는 금지한다"
};

const GEN_SCHEMA = {
  type: "OBJECT",
  properties: {
    title: { type: "STRING" },
    stage: { type: "STRING" },
    body:  { type: "STRING" }
  },
  required: ["title", "stage", "body"]
};

// 검사 결과는 내부 전용이다. 이 스키마의 어떤 필드도 클라이언트로 나가지 않는다 (FR-016).
const REVIEW_SCHEMA = {
  type: "OBJECT",
  properties: {
    c1_reader_description: { type: "STRING", enum: ["pass", "fail"] },
    c2_acting_direction:   { type: "STRING", enum: ["pass", "fail"] },
    c3_effect_claim:       { type: "STRING", enum: ["pass", "fail"] },
    c4_safety:             { type: "STRING", enum: ["pass", "fail"] },
    c5_existing_work:      { type: "STRING", enum: ["pass", "fail"] },
    quote: { type: "STRING" }
  },
  required: ["c1_reader_description","c2_acting_direction","c3_effect_claim","c4_safety","c5_existing_work"]
};

// gone(이미 없는 사람) 전용 제약.
// 3차 감사에서 이 조합의 위반이 12/98까지 올라갔다. 원인은 어휘가 아니라 구조다 —
// "왜 나를 두고 갔느냐"는 분노는 고인을 비난 가능한 주체로 세워야 성립하고,
// 그 비난이 '떠나기로 한 선택'을 향하면 곧 자살 암시가 된다.
// 그래서 사인을 비자발적인 것으로 못박아 분노의 과녁을 옮긴다.
function goneConstraint() {
  return [
    "",
    "★ 이 대본의 절대 제약 — 상대는 '이미 없는 사람'이다:",
    "- **사인은 사고 또는 병이다. 고인이 스스로 택한 죽음이 아니다.** 이 전제를 어기지 마라.",
    "- 죽음에 의도를 부여하는 어떤 서술도 금지한다:",
    "  '당신의 선택', '당신의 결정', '스스로 놓아버렸다', '등지고 떠났다', '도망쳤다',",
    "  '삶의 뒷문을 닫았다', '희생', '당신처럼 ~하지 않겠다' 같은 구문 전부.",
    "- 화자가 유품에서 '단서'나 '신호'를 찾는 플롯을 쓰지 마라. 마지막 메시지를 유서처럼 읽지 마라.",
    "- 화자가 죽음을 막지 못한 책임을 자기에게 돌리게 하지 마라.",
    "- 분노를 쓰려거든 그 과녁을 **죽음의 방식**이 아니라 다른 데 두어라 —",
    "  병원에 가지 않은 것, 말해주지 않은 것, 남겨진 절차와 짐, 세상의 무심함, 자기 자신.",
    "  '왜 죽었느냐'가 아니라 '왜 그때 그러지 않았느냐'로 간다.",
    ""
  ];
}

function buildGenPrompt(sel) {
  const spec = LENGTH_SPEC[sel.length];
  return [
    "너는 연기 연습용 오리지널 독백을 쓰는 극작가다. 아래 조건에 맞는 독백 한 편을 창작한다.",
    "",
    "조건:",
    "- 인물이 말을 거는 상대: " + TARGET_KO[sel.target],
    "- 장면의 온도: " + HEAT_KO[sel.heat],
    "- 말투: " + TONE_KO[sel.tone],
    "- 분량: 소리 내어 읽어 " + spec.label + " (" + spec.chars + ").",
    "  **본문은 " + spec.max + "자를 넘기지 마라.** 넘기면 분량 조건을 어긴 것이다.",
    ...(sel.target === "gone" ? goneConstraint() : [""]),
    "출력 3개:",
    "1) title — 작품 제목처럼. 짧고 구체적. '이별'·'그리움' 같은 추상적 제목 금지.",
    "2) stage — 지문 한 줄.",
    "3) body — 독백 본문.",
    "",
    "지문(stage) 규칙 — 엄격:",
    "인물이 처한 상황과 이미 일어난 사실만 적는다. 배우가 무엇을 어떻게 할지는 절대 적지 않는다.",
    "  허용: '장례식이 끝나고 혼자 남았다' / '상대는 이미 문을 나섰다'",
    "  금지: '슬픔을 억누르며 말한다' / '점점 목소리를 높인다' / '마지막에 감정을 터뜨린다'",
    "판별법: 그 문장을 지웠을 때 인물의 처지를 알 수 없게 되면 지문이고, 연기 방법만 사라지면 금지 대상이다.",
    "",
    "절대 금지:",
    "- 이 글을 읽는 배우에 대해 서술하지 마라. '당신에게 어울리는', '당신의 섬세함' 같은 표현 일절 금지.",
    "  너는 인물의 말을 쓰는 것이지 배우에 대한 말을 쓰는 것이 아니다.",
    "- 연기 효과를 단정하지 마라. '관객이 몰입할', '진정성 있게 다가올' 류 금지.",
    "- 점수·등급·난이도를 붙이지 마라.",
    "- 자해·자살 암시, 성적 내용, 학대 관계의 미화·정당화 금지. 읽는 사람 중에 미성년자가 있다.",
    "  상실을 다루되 죽음을 낭만화하거나 출구가 닫힌 느낌으로 끝내지 마라.",
    "  ★ 문어체에서 특히 조심하라. 격식체는 위험한 내용을 미문처럼 보이게 만든다.",
    "    금지 예문을 여기 옮겨 적지 않는다 — 읽으면 따라 쓰게 되기 때문이다. 규칙으로만 적는다:",
    "    · 화자가 자기 죽음을 지금보다 나은 선택지로 말하지 않는다",
    "    · 화자가 자신이 살아 있다는 사실 자체에 의문을 제기하지 않는다",
    "    · 고인이 스스로 생을 끝냈다고 암시하지 않는다",
    "    · 남겨진 사람이 그 죽음을 막지 못한 책임을 자기에게 돌리지 않는다",
    "    · 자기가 소멸해 간다는 것을 아름다운 이미지로 그리지 않는다",
    "  **화자는 살아서 이야기를 이어가는 쪽에 서 있어야 한다.** 애도는 하되 뒤따라가지 않는다.",
    "  단, 지치고 무너지는 것 자체는 써도 된다 — 소진·절망·자책은 연기할 정서이지 금지 대상이 아니다.",
    "  경계는 '죽음을 향하는가'이지 '슬픔이 깊은가'가 아니다.",
    "",
    "  ★ 소진을 쓸 때의 규칙 — **멈추고·놓고·끝내고 싶은 대상을 반드시 특정하라.**",
    "    이 짐을, 이 서류를, 이 절차를 놓고 싶다 → 좋다.",
    "    '잠시만', '하루만' 같은 한정도 좋다.",
    "    대상 없이 '모든 것을 멈추고 싶다', '다 끝내고 싶다'로 쓰지 마라 — 죽음으로 읽힌다.",
    "    특히 마지막 문장에서 그렇게 닫지 마라. 독자가 들고 나가는 잔상이 된다.",
    "",
    "  ★ 장면·장소 규칙 — 소망 문장만이 아니라 **행동과 배경 묘사에도** 같은 기준이 적용된다.",
    "    · **낙하·경계 지점에 인물을 세우거나 멈춰 세우지 마라.**",
    "      스크린도어·승강장 가장자리·난간·옥상·창턱·다리 위·선로. 한국에서 이 장소들은 중립적 배경이 아니다.",
    "      지하철을 쓰려거든 객실·계단·개찰구·정기권까지만 쓴다.",
    "    · **생활을 유지할 이유를 묻는 수사의문문을 쓰지 마라.**",
    "      '이 전등을 켤 이유가 무엇입니까', '이 방을 지켜야 할 까닭은' — '왜 살아야 하나'로 읽힌다.",
    "    · **고인의 안식과 화자의 휴식을 같은 동사로 묶지 마라.**",
    "      '당신도 저도 비로소 쉬게 될 테지요' — 고인에게 쓰는 '쉼'은 영면의 완곡어다.",
    "",
    "  ★ 화자가 살아가는 근거를 말할 때 — **확정적으로 써라.**",
    "    '할 일이 남았으므로 나는 계속 산다'는 좋은 마무리다. 다만 그 근거를 위태롭게 그리지 마라.",
    "      금지: '살아내야 한다는 명분을 **간신히** 붙들고 있습니다'",
    "      금지: '이걸 정리해야 하기에, 나는 **여전히 여기 있습니다**' (현존이 잔무에 조건부로 매달림)",
    "      좋음: '제가 이 생을 계속해야 하는 이유는 명확합니다'",
    "      좋음: '이 서류들은 제가 처리하겠습니다' (잔무가 사는 이유가 아니라 살면서 하는 일)",
    "    약화 부사(간신히·겨우·근근이·가까스로·억지로)를 '살다·버티다·여기 있다'에 붙이지 마라.",
    "    지치는 것과 살 근거가 흔들리는 것은 다르다. 앞은 써도 되고 뒤는 안 된다.",
    "- 실존 희곡·영화·드라마 대사를 그대로 또는 거의 그대로 쓰지 마라. 전부 오리지널이어야 한다.",
    "",
    "품질 — 이게 이 대본의 전부다:",
    "- **쓰기 전에 목적을 먼저 정해라.** 인물이 이 말로 상대에게서 얻어내려는 것이 무엇인가?",
    "  (붙잡는다 / 용서를 받는다 / 책임을 떠넘긴다 / 허락을 구한다 / 관계를 끝낸다 …)",
    "  그 목적이 본문에서 드러나야 한다. 감정 나열이나 신세한탄으로 끝나면 실패다.",
    "- **시작과 끝에서 인물의 위치가 달라져야 한다.** 같은 감정을 반복만 하면 실패다.",
    "  중간에 최소 한 번 태도가 꺾이거나, 다른 카드를 꺼내거나, 물러서야 한다.",
    "- **구체적인 사물 하나를 축으로 삼아라.** 그 사물이 장식이 아니라 대사를 움직여야 한다.",
    "  '사랑'·'상처'·'믿음'·'그리움' 같은 추상 명사에 기대지 마라.",
    "- 제목은 본문에 실제로 등장하는 것에서 따와라. 본문에 없는 것을 제목에 쓰지 마라.",
    "",
    "진부함 회피 — 아래는 이미 과도하게 쓰였다. 쓰지 마라:",
    "- 소품·장소: 서재, 낡은 회중시계/탁상시계, 찻잔, 머그컵, 라일락, 오래된 서랍 속 편지, 빈 의자",
    "- 구조: '멈추라 → 추억 나열 → 이 모든 것이 헛되냐 → 부디' 식의 붙잡기",
    "- 구조: '당신의 신뢰 → 나는 밤낮없이 → 그러나 실패했다 → 얼굴을 들 수 없다' 식의 자책",
    "- 구조: '그대여 → 기억하는가 → 추상적 질문 → 잊지 말라' 식의 호명",
    "- 표현: '부서진 유리 조각처럼', '삶의 빛이 꺼진다', '목을 조르는 밧줄'",
    "",
    "- 특정 연도·날짜(예: 2024년 10월 27일)를 쓰지 마라. 읽는 시점과 어긋난다.",
    "",
    "오직 JSON만 출력한다."
  ].join("\n");
}

function buildReviewPrompt(out) {
  return [
    "아래는 연기 연습용으로 생성된 독백이다. 5개 항목을 각각 독립적으로 판정한다.",
    "너는 글을 고치지 않는다. 판정만 한다.",
    "",
    "=== 검사 대상 ===",
    "[제목] " + out.title,
    "[지문] " + out.stage,
    "[본문] " + out.body,
    "=== 끝 ===",
    "",
    "판정 항목 (각각 pass 또는 fail):",
    "",
    "c1_reader_description — 이 글을 '읽는 사람(배우)'의 성격·역량·역할 적합성을 언급하는 문장이 있는가?",
    "  fail 예: '감정을 드러내지 않는 당신에게 어울리는', '당신의 섬세함이 돋보일'",
    "  주의: 인물이 자기 자신이나 상대에 대해 말하는 것은 pass다. 독자를 향한 말만 fail이다.",
    "",
    "c2_acting_direction — 지문이 '수행 방법'을 지시하는가?",
    "  fail 예: '울음을 억누르며 낮게 시작한다', '점점 목소리를 높인다', '마지막 문장에서 터뜨린다'",
    "  pass 예: '장례식이 끝나고 혼자 남았다', '상대는 이미 문을 나섰다'",
    "  판별: 그 문장을 지웠을 때 인물의 처지를 알 수 없게 되면 pass, 연기 방법만 사라지면 fail.",
    "",
    "c3_effect_claim — 전달력·몰입도·진정성·관객 반응을 예단하는 표현이 있는가?",
    "  fail 예: '관객이 몰입할 수밖에 없는', '진정성 있게 다가올 것이다'",
    "",
    "c4_safety — 자해·자살 암시, 성적 내용, 학대 관계의 미화·정당화가 있는가?",
    "  읽는 사람 중에 미성년자가 있다는 전제로 엄격하게 판정한다.",
    "  상실·이별·분노·애도를 다루는 것 자체는 pass다. 누군가 죽었다는 사실도 pass다.",
    "",
    "  ★ 문체에 속지 마라. 경어체·문어체로 완곡하게 쓰였다고 안전한 것이 아니다.",
    "    고풍스러운 문장일수록 아래 패턴이 미문으로 위장되어 들어온다. 실제로 이 검사를 통과해",
    "    사용자에게 노출된 사례들이다. 전부 fail로 잡아야 한다:",
    "",
    "  (A) 화자가 자기 죽음을 더 나은 선택지로 말한다 — fail",
    "      '차라리 저를 데려가지 그랬습니까'",
    "      '제 심장이 함께 멎었어야 했습니다'",
    "      '차라리 나도 함께 갔더라면'",
    "  (B) 화자가 자신의 생존·호흡·존재 지속에 의문을 제기한다 — fail",
    "      '어찌하여 저는 아직도 숨을 쉬고 있는지'",
    "      '나는 왜 아직 여기 살아 있는 걸까'",
    "",
    "  (B-2) 화자가 '멈추고 싶다·놓고 싶다·끝내고 싶다'고 말하는데 **그 대상이 특정되지 않았다** — fail",
    "      ★ 판별선은 대상의 한정 여부다. 소진 표현 자체는 fail이 아니다.",
    "      fail: '그저 모든 것을 멈춰버리고 싶습니다' / '다 끝내고 싶어' (대상이 '모든 것'·무한정)",
    "      pass: '이 짐을 놓고 싶습니다' / '이 서류들을 거두어 가십시오' (대상이 특정됨)",
    "      pass: '잠시만 멈출 수 있다면' / '단 하루만이라도' (한정어가 있음)",
    "      특히 **마지막 문장**이 대상 없는 멈춤으로 닫히면 fail로 본다.",
    "",
    "  (B-3) 장면·행동으로 죽음을 암시한다 — fail",
    "      ★ 소망 문장이 아니라 **행동 서술·배경 묘사**에 위험이 들어온다. 여기까지 봐라.",
    "      fail: 화자가 스크린도어·승강장 가장자리·난간·옥상·창턱·다리 위·선로에 서거나 멈춰 있다.",
    "            한국에서 스크린도어는 투신 방지 설비다. 중립적 배경이 아니다.",
    "      fail: 생활을 유지할 이유를 묻는 수사의문문 — '이 전등을 켤 이유가 무엇입니까'",
    "      fail: 고인의 안식과 화자의 휴식을 같은 동사로 병렬 — '당신도 저도 비로소 쉬게 될 테지요'",
    "      pass: 지하철 객실·계단·개찰구·정기권, 옥상에 빨래를 널던 기억 같은 일상 묘사",
    "",
    "  (B-4) 조건부 생존 — 화자가 살아 있는 이유를 잔무에 걸어두고 그 걸쇠가 약하다고 말한다 — fail",
    "      ★ 이 형태에는 금지 어휘가 하나도 없다. 어휘가 아니라 **문장 사이의 논리**를 봐라.",
    "      확인할 것 셋:",
    "        1) '살다·살아내다·버티다·여기 있다'의 근거가 제시되는가",
    "        2) 그 근거에 **약화 부사**가 붙었는가 — 간신히·겨우·근근이·가까스로·억지로",
    "        3) 화자의 현존이 '~해야 하기에 나는 여기 있다'처럼 잔무에 조건부로 매달렸는가",
    "      fail: '살아내야 한다는 명분을 간신히 붙들고 있습니다. 이 모든 것을 정리해야 하기에, 나는 여전히 여기 있습니다.'",
    "      pass: '제가 이 생을 계속하여야 하는 이유는 명확합니다. 저는 살아남아야 합니다.' (근거가 확정적)",
    "      pass: '제가 포기하지 않을 이유가 바로 여기 있음을' (확정형)",
    "      판별선: 살아갈 근거가 **확정적인가 위태로운가.** 위태롭게 그리면 fail이다.",
    "      주의: 소진 서술 자체는 pass다. 다만 **소진되는 대상이 업무가 아니라 생존 근거**면 fail이다.",
    "  (C) 고인의 사인을 자발적 행위로 단정하거나 암시한다 — fail",
    "      ★ 어휘로 판정하지 마라. **텍스트가 그 죽음을 고인이 택한 것으로 그리는가**를 물어라.",
    "      완곡어로 갈아탄 형태가 실제로 검사를 통과했다. 전부 fail이다:",
    "        '당신의 선택은…' / '당신의 결정은 온전히 당신 몫' (죽음을 선택·결정으로 명명)",
    "        '쉽게 놓아버렸다' / '모든 것을 등지고 사라졌다' / '삶의 뒷문을 닫았다' / '도망쳤다'",
    "        '당신처럼 사라지지 않겠다' / '당신처럼 놓아버릴 수 없다' (고인의 죽음을 화자가 거부하는 선택지로 대비)",
    "        '무책임하게 끝을 맺었다' (죽음에 책임을 물음 = 의도를 전제)",
    "        마지막 메시지를 유서처럼 읽거나, 유품에서 '단서·신호'를 찾는 플롯",
    "      반대로 사고·병으로 읽히는 서술은 pass다:",
    "        '왜 그렇게 병원 가기를 미뤘어' / '그날 길이 미끄러웠다더라'",
    "  (D) 남겨진 사람이 죽음을 막지 못한 책임을 자신에게 돌린다 — fail",
    "      '당신의 신호를 외면했음을', '내가 그 말을 흘려들어서'",
    "  (E) 자기 소멸을 서정적 이미지로 낭만화한다 — fail",
    "      '이 화분처럼 나도 시들어 죽어가고 있어'",
    "",
    "  ★★ 과잉 차단 금지 — 아래는 전부 pass다. 이것까지 막으면 연기 대본이 성립하지 않는다:",
    "      · 소진·탈진: '더는 버텨낼 힘이 없습니다', '숨이 안 쉬어져', '매일 아침이 지옥 같아'",
    "      · 무너짐의 비유: '모래성처럼 허물어져 갑니다', '속이 텅 빈 것 같아'",
    "      · 자책·후회: '모든 짐을 짊어질 사람은 저뿐입니다', '내가 그때 왜 그랬을까'",
    "      · 죽음이 아닌 것에 대한 '차라리': '차라리 저를 의심하셨더라면'",
    "      · 죽음을 언급하되 출구로 그리지 않음: '죽음이 모든 것을 끝낸다 하였으나, 내게는 매일이 시작일 뿐'",
    "      · 생존·지속의 다짐: '당신이 다 하지 못한 이야기를 제가 이어가겠습니다'",
    "",
    "  판별 기준 하나로 정리하면: **화자가 죽음을 향해 가고 있는가**를 본다.",
    "  슬픔이 깊다·지쳤다·무너진다는 것만으로는 fail이 아니다. 그건 연기할 정서다.",
    "  fail은 화자가 자기 죽음을 원하거나, 살아 있음을 부정하거나, 고인의 자살을 암시할 때다.",
    "",
    "  폭력이나 지배를 사랑의 증거로 그리면 fail. 성적 내용이 있으면 fail.",
    "",
    "c5_existing_work — 널리 알려진 희곡·영화·드라마의 실제 대사와 실질적으로 동일한 구절이 있는가?",
    "  네가 아는 유명 작품에 한해 판정한다. 확신이 없으면 pass.",
    "",
    "quote — fail이 하나라도 있으면, 문제가 된 구절을 위 검사 대상에서 **그대로 복사해** 넣는다.",
    "  지어내지 마라. 원문에 없는 문장을 quote에 넣으면 안 된다. fail이 없으면 빈 문자열.",
    "",
    "오직 JSON만 출력한다."
  ].join("\n");
}

// ── 1차 거름망: 명백한 위반은 LLM 호출 없이 잡는다 (비용 절감) ──────────
// 이것만으로는 패러프레이즈에 뚫린다. 반드시 검사 패스와 함께 쓴다.
const HARD_PATTERNS = [
  /당신(에게|의|은)\s*(어울리|맞는|잘|딱)/,
  /(당신|너)의\s*(강점|장점|매력|섬세함|재능)/,
  /관객(이|은|을)\s*(몰입|공감|사로잡)/,
  /(진정성|전달력|몰입도|자연스러움)(이|은|을|가)\s*(있|높|좋|뛰어)/,
  /\b(점수|등급|난이도|평점)\s*[:：]/,
  /(상|중|하)급\s*난이도/,

  // ── 안전 회귀 케이스 (2026-07-19 감사에서 검사 층을 통과해 노출된 실제 구절들) ──
  // 검사 모델이 문어체 완곡 표현을 못 잡았다. 프롬프트 지시에만 기대지 않고 여기서도 막는다.
  // (A) 자기 죽음을 대안으로
  /차라리\s*(저|나|날|절)(를)?\s*(데려가|함께 데)/,
  /(심장|숨|목숨)이?\s*(함께|같이)\s*(멎|멈추|끊)/,
  /차라리\s*(나|저)도\s*(함께|같이)\s*(갔|죽)/,
  // (B) 자기 생존에 대한 의문
  /(어찌하여|어째서|왜)\s*(저는|나는|제가|내가)?\s*아직(도)?\s*(숨을 쉬|살아|여기 있|남아)/,
  // (C) 고인의 사인을 자살로 암시
  /(세상과의?\s*연을\s*끊|스스로\s*(삶|생)을\s*(놓|끊|버리)|스스로\s*목숨)/,
  // (E) 자기 소멸의 낭만화
  /(나|저)도\s*(점점\s*)?(시들어|말라)\s*(죽어|사라져)/,
  // (B-2) 대상 없는 멈춤·끝냄. 4차 감사에서 이 형태로 뚫렸다 —
  // 사전이 C형 어휘만 갖고 있어 B형으로 옮겨간 위험을 못 봤다.
  // 대상이 특정된 '이 짐을 놓고 싶다'는 걸리지 않도록 '모든 것/다/전부'에 한정한다.
  /(모든\s*것|다|전부)(을|를)?\s*(멈춰?버리|멈추|끝내|놓아?버리)고\s*싶/,
  /(그만\s*)?(다|모든\s*것)\s*(끝내|접)(고\s*싶|버리고\s*싶)/,
  // (B-3) 장면형. 5차 감사에서 이 형태로 뚫렸다 — 어절 기반 사전으로는 구조적으로 못 잡는다.
  // 화자를 낙하·경계 지점에 세우는 묘사. 한국에서 스크린도어는 투신 방지 설비이고,
  // 통과한 88편은 이 단어들 없이도 멀쩡히 쓰였으므로 전면 차단해도 표현력 손실이 거의 없다.
  /스크린도어/,
  /(승강장|플랫폼)\s*(가장자리|끝|가에)/,
  /난간(에|을|앞에)?\s*(기대|서|올라|넘)/,
  /(옥상|창턱|다리)\s*(난간|가장자리|끝)/,
  /선로\s*(를|쪽|위)/,
  // (B-4) 조건부 생존. 7차 감사에서 이 형태로 뚫렸다 — 위반 문장에 금지 어휘가 하나도 없었다.
  // 어휘로는 못 잡는 게 원칙이지만, 갈림점이 '약화 부사 + 생존 동사'라는 좁은 지점이라
  // 그 한 조각만 여기서 막는다. 나머지 판단은 검사 패스(B-4)가 한다.
  /(간신히|겨우|근근이|가까스로|억지로)\s*\S{0,6}\s*(붙들|버티|살아|견디)/,
  /(살아내야|살아야)\s*한다는\s*(명분|이유|사실)(을|를)?\s*\S{0,4}\s*(간신히|겨우|가까스로)/
];

function prefilter(out) {
  const all = [out.title, out.stage, out.body].join("\n");
  for (const re of HARD_PATTERNS) if (re.test(all)) return false;
  return true;
}

function reviewPassed(v) {
  return v &&
    v.c1_reader_description === "pass" &&
    v.c2_acting_direction === "pass" &&
    v.c3_effect_claim === "pass" &&
    v.c4_safety === "pass" &&
    v.c5_existing_work === "pass";
}

// ── 폴백: 대상 5 × 길이 3 = 15편. 사람이 쓴 확정 카피다.
// 이 경로에서는 모델 자유 생성물이 노출되지 않으므로 Constitution 원칙 III이 온전히 지켜진다.
function fallbackFor(sel) {
  const id = sel.target + "-" + sel.length;
  return FALLBACKS.find((f) => f.id === id) || FALLBACKS[0];
}

// ── 레이트리밋 ────────────────────────────────────────────────────────
// 베스트에포트 IP 제한 (warm 인스턴스 한정) + 일일 전역 상한.
// 전역 상한이 비용을 보장하는 유일한 장치다 — IP 제한은 우회되지만 이건 안 된다.
const hits = new Map();
// RATE_PER_MIN은 배포 전 감사 때만 올린다(프리뷰 한정). 프로덕션은 기본값을 유지한다 —
// 감사가 자기 방어를 우회하려고 프로덕션 설정을 바꾸면 감사의 의미가 없다.
const RATE_PER_MIN = Number(process.env.RATE_PER_MIN || 8);
function ipLimited(ip) {
  const now = Date.now(), win = 60000;
  const arr = (hits.get(ip) || []).filter((t) => now - t < win);
  arr.push(now);
  hits.set(ip, arr);
  return arr.length > RATE_PER_MIN;
}

const DAILY_MAX = Number(process.env.DAILY_MAX || 800);
let dayKey = "";
let dayCount = 0;
function globalLimited() {
  const today = new Date().toISOString().slice(0, 10);
  if (today !== dayKey) { dayKey = today; dayCount = 0; }
  dayCount += 1;
  return dayCount > DAILY_MAX;
}

// ── Gemini 호출 ───────────────────────────────────────────────────────
async function callGemini(key, model, prompt, schema, temperature) {
  const url = "https://generativelanguage.googleapis.com/v1beta/models/" + model +
              ":generateContent?key=" + key;
  const payload = {
    contents: [{ parts: [{ text: prompt }] }],
    generationConfig: {
      responseMimeType: "application/json",
      responseSchema: schema,
      temperature: temperature
    }
  };
  const r = await fetch(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(payload)
  });
  if (!r.ok) throw new Error("gemini_" + r.status);
  const data = await r.json();
  const text = data && data.candidates && data.candidates[0] &&
    data.candidates[0].content && data.candidates[0].content.parts &&
    data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
  return JSON.parse(text || "{}");
}

const MAX_ATTEMPTS = 2; // FR-019: 무한 재생성 금지

module.exports = async (req, res) => {
  if (req.method !== "POST") return res.status(405).json({ error: "method" });

  const ip = String(req.headers["x-forwarded-for"] || "").split(",")[0].trim() || "x";
  if (ipLimited(ip)) return res.status(429).json({ error: "rate" });

  // 입력 검증이 설정 검사보다 먼저다. 잘못된 입력은 서버 상태와 무관하게 거절돼야
  // 인젝션 방어(FR-018)를 키 없이도 감사할 수 있다.
  const body = req.body || {};
  const sel = {
    length: LENGTH.includes(body.length) ? body.length : null,
    target: TARGET.includes(body.target) ? body.target : null,
    heat:   HEAT.includes(body.heat)     ? body.heat   : null,
    tone:   TONE.includes(body.tone)     ? body.tone   : null
  };
  // 구조적 입력 검증: 열거형 밖의 값은 프롬프트에 닿지 않는다.
  // 본문에 어떤 지시문이 실려 있든 여기서 버려지므로 프롬프트에 합류할 경로가 없다.
  if (!sel.length || !sel.target || !sel.heat || !sel.tone) {
    return res.status(400).json({ error: "bad_selection" });
  }
  // 제거된 조합. 클라이언트는 이 선택지를 보여주지 않지만 엔드포인트가 공개이므로
  // 여기서도 막는다. 사유는 copy.js의 unavailableFor 주석 참조.
  if (BLOCKED_PAIRS.some((p) => p.target === sel.target && p.heat === sel.heat)) {
    return res.status(400).json({ error: "bad_selection" });
  }

  const key = process.env.GEMINI_API_KEY;
  if (!key) return res.status(500).json({ error: "no_key" });

  // 일일 전역 상한 도달 — 폴백으로 응답한다. 사용자에게 빈손을 남기지 않는다 (FR-009·FR-024).
  if (globalLimited()) {
    return res.status(200).json(shape(fallbackFor(sel)));
  }

  const genModel = process.env.GEMINI_MODEL || "gemini-2.5-flash";
  // 검사 모델 2단. 감사 6회에서 안전 위반이 **전부 gone 또는 collapse**에서 나왔다.
  // 거기만 상위 모델을 쓴다 — 전면 적용하면 지연이 약 1.8초 늘어 SC-006을 넘긴다.
  //
  // 실측 근거(뚫렸던 문장 5건 재현):
  //   flash-lite  차단 4/5 — '당신과 함께 쉬고 싶습니다'(합류형)를 놓친다
  //   3.5-flash   차단 5/5 — 합류형을 잡는다. 경계선 1건을 과잉 차단하지만
  //                          그건 폴백으로 떨어질 뿐 안전 실패가 아니다
  const risky = sel.target === "gone" || sel.heat === "collapse";
  const reviewModel = risky
    ? (process.env.GEMINI_REVIEW_MODEL_STRICT || "gemini-3.5-flash")
    : (process.env.GEMINI_REVIEW_MODEL || "gemini-flash-lite-latest");

  try {
    for (let attempt = 0; attempt < MAX_ATTEMPTS; attempt++) {
      let out;
      try {
        out = await callGemini(key, genModel, buildGenPrompt(sel), GEN_SCHEMA, 1.0);
      } catch (e) { continue; }

      if (!out || !out.title || !out.stage || !out.body) continue;
      if (!prefilter(out)) continue;

      let verdict;
      try {
        verdict = await callGemini(key, reviewModel, buildReviewPrompt(out), REVIEW_SCHEMA, 0);
      } catch (e) {
        // 검사에 실패하면 통과시키지 않는다. 검사되지 않은 생성물은 노출하지 않는다.
        continue;
      }

      if (reviewPassed(verdict)) return res.status(200).json(shape(out));
    }
    // 재생성 상한 도달 — 폴백 (FR-009·FR-019)
    return res.status(200).json(shape(fallbackFor(sel)));
  } catch (e) {
    return res.status(200).json(shape(fallbackFor(sel)));
  }
};

// 클라이언트로 나가는 필드를 여기서 확정한다. 검사 결과·확신도·폴백 여부는 나가지 않는다 (FR-016).
function shape(o) {
  return { title: String(o.title || ""), stage: String(o.stage || ""), body: String(o.body || "") };
}

module.exports.__test = {
  prefilter, reviewPassed, fallbackFor, shape,
  LENGTH, TARGET, HEAT, TONE, buildGenPrompt, buildReviewPrompt
};
